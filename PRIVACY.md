# 隐私与权限说明

## 概览

顶屿（TopIslet）当前没有账号系统、云同步、广告 SDK 或遥测服务。音乐、日历和提醒事项数据在本机处理，不会由本项目上传到自有服务器。

## 数据处理

### 音乐信息

- 通过本机 MediaRemote Adapter 读取汽水音乐报告的歌名、歌手、专辑、封面、播放状态和进度。
- 封面可能来自 MediaRemote 返回的本地数据，也可能按汽水提供的封面 URL 由系统网络组件加载。
- Apple Music Alpha 支持通过 macOS Apple Event 按当前 Music 进程读取歌曲、播放状态和进度；不会使用系统全局当前媒体作为控制目标。
- 网易云音乐 Alpha 支持通过本机 MediaRemote Adapter 按 `com.netease.163music` 当前进程读取歌曲、封面、播放状态和进度；不会把系统全局当前媒体作为网易云状态或控制目标。
- Apple Music 电台曲目未提供内嵌封面时，顶屿会将当前歌名与歌手通过 HTTPS 发送给 Apple 的公开 `itunes.apple.com/search` 接口，并仅从 `*.mzstatic.com` 下载唯一精确匹配的封面。专辑、播放状态、进度、曲目 ID 和控制信息不会随该查询发送；查询不经过本项目自有服务器。
- 项目不会抓取汽水账号 token，不进行网络中间人代理或修改汽水 App。

### 辅助功能

- 辅助功能是汽水与网易云音乐播放、暂停和切歌产品控制的必要权限，也用于用户主动执行的诊断；MediaRemote 负责定向状态读取。
- 控制只绑定当前目标音乐应用的 Bundle ID 和 PID；汽水要求唯一语义控件组，网易云只使用其原生“控制”菜单。目标结构不符合预期时不会执行控制。
- 项目不使用固定屏幕坐标，不合成鼠标点击，也不会为音乐控制切换前台 App。

### Apple Music 自动化

- Apple Music 适配默认开启，可在“设置 → 音乐”中关闭；关闭后顶屿会停止对应观察、读取、封面查询和控制。
- 只有用户允许“自动化 - Apple Music”后，顶屿才会读取或控制 Apple Music。
- 控制请求绑定当前 Apple Music 的 Bundle ID、进程 ID 和曲目身份；不会回退到系统全局媒体键，也不会自动启动 Apple Music。

### 日历与提醒事项

- 日历与提醒事项权限分别申请，默认关闭，不会在首次启动时自动弹出授权。
- 日历只查询临近开始的定时事件，默认忽略全天事件。
- 提醒事项只查询具有具体时间且刚到期的未完成事项。
- 事项级去重标识保存在本机 `UserDefaults`，最长保留约 48 小时，用于避免重复展示。

### 本机诊断日志

- 顶屿通过 macOS Unified Logging 记录低频、事件驱动的运行诊断，用于分析来源切换、切歌显示、封面到达、控制和进度跳转延迟。
- 曲目仅记录由来源、歌名和歌手生成的 12 位截断哈希指纹，以及歌手、封面、时长和歌词字段是否齐全；不写入完整歌名、歌手、歌词、封面数据或账号信息。
- 日报工具只在用户本机读取上述日志，并将来源切换次数、成功率和 P50/P95 延迟等聚合结果写入项目 `.build/qa/`；原始日志和聚合报告均不会由本项目上传。
- Unified Logging 的保留和清理由 macOS 管理；删除 App 不代表立即清除系统日志，可等待系统轮换或使用 macOS 自带日志管理方式处理。

## 不使用的能力

- 不请求屏幕录制权限。
- 不使用 OCR 读取歌词或播放状态。
- 不抓包、不进行 MITM、不提取账号 token。
- 不向项目自有服务器上传播放历史、日历内容、提醒内容或诊断日志；Apple Music 电台封面查询仅向 Apple 发送当前歌名与歌手。

## 撤销权限与清理

- 可在“系统设置 → 隐私与安全性”中撤销辅助功能、自动化、日历或提醒事项权限。
- 可在 App 设置中关闭日历和提醒事项接入。
- 删除 App 后，可按需运行 `defaults delete io.github.scorpioxyb.topislet` 清理顶屿的本机偏好设置。此操作会同时删除布局校准和功能开关配置。

## 变化

如果未来加入遥测、云服务或新的数据来源，本文件必须在功能发布前更新，并明确说明收集字段、用途、保留期限和退出方式。
