name: Build unsigned release candidate

on:
  workflow_dispatch:
    inputs:
      version:
        description: CFBundleShortVersionString, for example 0.1.1
        required: true
        default: 0.1.1
      tag:
        description: Git tag, for example v0.1.1-alpha.2
        required: true
permissions:
  contents: read

jobs:
  release:
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.tag }}

      - name: Verify release source
        env:
          RELEASE_TAG: ${{ inputs.tag }}
          VERSION: ${{ inputs.version }}
        run: bash Scripts/verify-release-source.sh

      - name: Build release candidate
        env:
          VERSION: ${{ inputs.version }}
          BUNDLE_ID: io.github.scorpioxyb.topislet
          SIGN_IDENTITY: "-"
        run: bash Scripts/build-release.sh

      - name: Verify release candidate
        env:
          VERSION: ${{ inputs.version }}
        run: bash Scripts/verify-release-dmg.sh .build/release-artifacts/TopIslet-v${{ inputs.version }}-arm64.dmg

      - name: Upload candidate artifact
        uses: actions/upload-artifact@v7
        with:
          name: TopIslet-${{ inputs.tag }}
          path: |
            .build/release-artifacts/TopIslet-*.dmg
            .build/release-artifacts/TopIslet-*.dmg.sha256
          if-no-files-found: error
