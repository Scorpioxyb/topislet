# 顶屿 TopIslet v0.1.1-alpha.1

本版本继续作为 GitHub Alpha 开发者预览版发布，集中改善汽水音乐适配，不新增其他播放器、计时器或通知能力。

## 主要变化

- 播放与暂停点击立即反馈，真实汽水状态在后台确认，失败时安全回滚。
- 修复启动、展开和切歌时旧时间样本覆盖新样本的问题。
- 修复暂停后进度继续前进、回退或被 AX 旧进度覆盖的问题。
- 汽水定向控制不再依赖 `/usr/bin/python3`，未安装 Xcode 也不影响 App 运行时控制。
- MediaRemote 报告“已派发”后，顶屿会在派发完成约 120/280/520ms 定向回读汽水真实状态；至少 500ms 后，只有累计取得两个不同的命令后 fresh 样本且均明确显示操作未生效，才尝试一次唯一语义 AX 控件。最终没有样本、只有一个样本、重复 sample、来源或 PID 异常以及旧 generation 均安全失败，不执行 AX，避免误双发。
- 修复 Electron AX 树把同一播放器栏包装成多个对象而导致的安全拒绝；仍无法唯一证明控件归属时继续拒绝操作。
- 保留辅助功能权限作为唯一语义控件的安全降级路径。

## 验证

- 31 项自动化回归测试通过，包括无 fresh 样本、单一或重复 sample、来源/PID 异常、两个不同 fresh 样本、500ms 确认窗口、目标状态确认及旧 generation 失效等安全边界。
- MediaRemote Adapter 从固定上游 commit 与项目补丁逐字节重建通过。
- 汽水窗口与顶屿并行采样的播放进度误差约 1 秒。
- 暂停后两端进度保持冻结；恢复播放与切歌状态一致。
- 安装包内不再包含 Python 控制脚本。

## 已知限制

- 当前只对汽水音乐进行产品级适配。
- 项目依赖 macOS 非公开 MediaRemote 能力，系统更新后需要重新验证。
- 本包为 ad-hoc 签名、未公证的 Alpha 预览版，首次打开可能出现 Gatekeeper 提示。
- 计时器和通知不属于本阶段优先范围。

## 安装

打开 `TopIslet-v0.1.1-arm64.dmg`，将“顶屿.app”拖到“Applications”快捷入口。

校验下载文件：

```bash
shasum -a 256 -c TopIslet-v0.1.1-arm64.dmg.sha256
```
