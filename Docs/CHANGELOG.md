# 变更记录

## Unreleased

### v0.2.0-dev 网易云音乐 Alpha 支持

- 新增按 `com.netease.163music` Bundle ID 与当前 PID 定向的网易云音乐专属状态桥，可读取歌名、歌手、专辑、封面、播放状态、进度和时长；系统当前媒体源与其他视频应用不能进入该快照。
- 新增网易云进程内原生“控制”菜单适配，播放 / 暂停、上一首和下一首均绑定点击时的 PID 与歌曲身份，不发送全局媒体键、不激活应用、不移动鼠标。
- 音乐来源路由扩展为汽水、网易云音乐和 Apple Music；前台已适配音乐应用立即切换，离开后按真实播放状态选择，未适配视频应用不参与控制目标。
- 网易云切歌增加事务门控：拒绝“旧歌名 / 旧封面 + 新时长”的混合快照，完整元数据与封面一起发布；不同封面可作为强确认，同封面歌曲继续双快照确认。
- 本机网易云音乐 `3.1.8 (3368)` 实测专属读取约 `42ms`，下一首和上一首完整原子切换分别为 `0.761s` 与 `0.758s`。
- 设置音乐页新增网易云运行、辅助功能、连接与可用控制状态；新增 `--netease-music-status`、`--netease-music-semantic-control` 和 `--netease-music-adapter-control` 诊断命令。
- 网易云 Alpha 当前进度保持只读，不声明或调用系统全局进度跳转。
- 网易云启动或重新激活时，专属 MediaRemote client 尚未就绪会执行 `350ms / 700ms / 1.2s` 有界短重试；稳态不增加轮询，应用退出后仍立即清除旧歌曲。
- QuickTime 或抖音抢占系统媒体焦点时，网易云同曲稀疏事件不再把播放态改成暂停或清空时间线与封面；岛保持最近权威状态并触发完整 PID 定向回读。实测岛内暂停网易云时，QuickTime 和抖音继续独立播放。
- Apple Music 连续切换同一艺人、同一专辑的歌曲时复用最近已验证封面，并写入新曲目身份缓存；避免相同封面重复请求 Apple 目录和约半秒的占位图阶段。不同艺人、不同专辑或缺失专辑身份时严格拒绝复用。
- 当前 127 项自动化回归测试通过，开发安装包重新签名与严格验证通过。

### v0.1.1-alpha.4 Apple Music Alpha 支持

- 顶部胶囊和展开主体由双窗口重构为单一 `NSPanel`，展开/收回始终保持顶边和屏幕中心不变，悬停与外部点击使用明确的 Header、Body 和窄桥区域。
- 恢复单窗口从紧凑尺寸到展开尺寸的连续插值轨迹，保留顶部与水平中心锚定；窗口整体长出并带动内容形变，不再瞬时切换透明画布。
- 悬停展开响应缩短到约 50ms，保持单窗口连续形变；快速反向切换仍由动画代次保证最终位置和尺寸正确。
- 动画完成回调增加代次与目标模式双重门禁，快速反向切换时旧 completion 不再覆盖最新窗口 frame。
- CI 开始运行 `swift test`；Release 候选增加精确 tag、干净工作区与测试门禁，DMG 校验增加版本号和构建号。
- 修复 Documents 文件提供器为开发包重新附加扩展属性时阻断 `/Applications/顶屿.app` 安装验证的问题。
- 修复汽水窗口最小化后 AX 树退化导致三种控制全部安全拒绝：只对唯一标准窗口后台临时恢复，控制完成后重新最小化，不激活汽水、不移动鼠标。
- 独立抖音 App 前台播放回归通过：视频持续播放，汽水暂停/恢复、下一首和上一首均只命中汽水。
- 新增按 Apple Music 进程 PID 定向的 Apple Event 适配器，可读取歌曲、播放状态和时间轴，并支持播放 / 暂停、上一首、下一首和绝对进度跳转；不会自动启动 Music，也不会使用系统当前媒体控制。
- 新增汽水与 Apple Music 自动来源选择：两者同时播放时汽水优先；汽水暂停而 Apple Music 播放时自动切换；退出应用或进程实例变化时立即废弃旧路由。
- 新增前台应用优先路由：切到汽水窗口时岛立即显示汽水，切到 Apple Music 时立即显示 Apple Music；离开两个音乐应用后恢复播放状态兜底。控制目标与岛当前显示来源保持一致。
- 按钮与进度操作绑定点击瞬间岛实际显示的来源：汽水固定到具体 PID，Apple Music 固定到具体 PID 与曲目身份；目标失效时安全拒绝，不再在点击后重新选择或改投其他应用。
- 前台应用身份先立即切换，歌曲与封面再异步补齐；前台 Apple Music 在权限或快照未就绪时仍保持 Apple Music 空闲状态。异步控制结果只在岛仍显示原应用时更新界面。
- Apple Music 新增定向专辑封面读取：按曲目身份缓存原始图片，只在缓存缺失时额外读取；读取前后歌曲 ID 不一致时丢弃结果，封面失败不会影响歌名、进度或控制。
- Apple Music 电台曲目没有脚本封面时，后台通过 Apple 公共 iTunes Search 按歌名、歌手和专辑唯一精确匹配，并从 Apple CDN 下载 600px 封面；模糊、歧义或非 Apple HTTPS 地址一律拒绝。
- 已确认处于“电台无原生封面”会话后，下一首的 `playerInfo` 通知会立即并行预取目录封面；权威 Apple Event 快照仍负责最终曲目身份门禁。普通原生封面则与切歌元数据在同一次快照返回，稳态轮询不读取封面。
- Apple Music 原生封面与切歌元数据改为单次定向快照，本机实测约 265-300ms 同时返回；电台目录封面仍异步补齐，只替换图片，不重锚进度、刷新控制验证时间或覆盖乐观播放状态。
- 封面请求标记会在请求发起时消费，旧请求完成不再清除执行期间到达的新切歌通知；岛内封面更新移除额外 160ms 淡入等待，数据到达后直接替换。
- `playerInfo` 切歌通知会立即启动绑定 PID 与完整曲目 ID 的原生封面预取，60ms 只用于合并 UI 刷新通知；避免轻量时间轴请求先发布长时间占位图，岛内上一首 / 下一首也不再同时追加第二个重型封面快照。
- 原生、目录与普通后台封面读取增加双向 in-flight 去重；目录失败遵守 30 秒临时失败和 5 分钟无匹配退避，区域端点超时后不再立即追加第二次全局超时。
- Apple Music 播放 / 暂停加入点击即刻的本地乐观反馈，实际 Apple Event 仍绑定点击瞬间的 PID、应用实例和曲目身份并串行执行；旧操作失败不会回滚新操作。
- Apple Music 轮询改为通知优先、单请求加完成时间节流：重复 `playerInfo` 通知先合并，播放中每 5 秒、暂停 10 秒、后台 15 秒才做一次兜底读取，控制确认仍使用短时定向回读；连续失败自动退避，避免高频 Apple Event 干扰 Music 电台与商店状态。
- Apple Music 电台切歌期间若同一进程的定向读取返回瞬时 `.degraded` 或 `409` 混合快照，保留最近可信歌曲并按 `250ms`、`600ms`、`1.2s`、`2.4s` 有界短重试；退出、PID 变化、权限错误和重试耗尽仍立即正常降级。ATEEZ 电台实测在 `683ms` 发布新歌、`1156ms` 补齐目录封面，期间不再回退到暂停的汽水。
- Apple Music 元数据、时间线、控制确认和异步封面快照统一经过同一准入门禁：先拒绝迟到快照，同进程瞬时失败继续有界重试，只有实际接受的新快照才取消待执行重试。控制确认会复用已经在途的权威元数据读取，不再并发追加重复的时间线 Apple Event。
- 一次 UI 发布只计算一次来源，避免歌曲内容和状态标签在防抖边界来自不同应用；Apple Music 迟到快照按时间戳拒绝覆盖更新状态。
- 设置的“音乐”页新增 Apple Music 自动化权限、连接、响应耗时和可用控制诊断；实时歌曲与进度统一显示在“当前活动”，避免重复状态逐渐分叉。
- 音乐页区分适配支持等级、启用状态、运行状态和权限状态；Apple Music 可关闭，关闭后停止观察、轮询、封面查询与控制。
- Apple Music 连接检查复用产品主适配器，并在应用退出、PID 变化或权限失效时清除旧连接结果。
- Apple Music 读取和控制增加生命周期代次；关闭、撤权或快速重开时，迟到读取、封面任务和排队控制不能重新进入当前状态。
- Apple Music 播放 / 暂停改用明确的播放或暂停目标命令，并增加同曲确认门控；点击后的乐观状态不会再被短暂返回的旧播放状态写回，确认、切歌、失败或主动超时刷新后自动解除。
- 播放 / 暂停主按钮使用单层轻微回弹和纯淡入淡出图标替换，不再叠加多层缩放与弹簧；保持本地乐观状态立即切换，不显示等待圆线，也不增加控制延迟。
- 音乐空状态不再响应悬停自动展开；真实歌曲、播放控制确认、计时器和有效提醒仍保留悬停展开。
- 空状态首次收到真实歌曲后自动切换为带封面紧凑音乐岛；后续刷新不会覆盖用户手动折叠、展开或临时活动。
- Apple Music 已验证快照在对应进程仍运行时持续有效，不再因暂停态低频轮询超过 8 秒而反复回落到空岛；空态重复刷新也不再重复触发折叠动画。
- 窗口形态动画增加最终 frame 校准；即使启动阶段动画被屏幕布局刷新打断，也会落到正确的顶部位置和紧凑宽度，不再出现状态已切换但窗口仍卡在空岛尺寸。
- 汽水 MediaRemote stream 增加可取消的单飞重启、进程代次门禁和 TERM/KILL 有界回收；停止、退出或快速重启后，旧回调不能重新发布状态，后台不再遗留孤儿 Perl 订阅进程。命令行收到 SIGINT / SIGTERM 时也会统一走应用退出清理。
- 汽水播放 / 暂停会生成新的临时 `contentItemIdentifier`；歌曲身份现改用 PID、歌名、歌手、专辑和时长的稳定组合。仅 UUID 变化的同曲事件直接合并，歌手、专辑、时长和封面不再短暂清空；真正稳定元数据变化仍进入原子切歌门控。
- 缺少稳定元数据的同名稀疏事件只允许更新播放态和时间轴，不修改曲目身份或刷新播放证据；汽水退出或 PID 切换后，旧 PID 在短期窗口内被拒绝，迟到事件不能覆盖重启后的新进程。
- 汽水进程集合为空时媒体身份校验改为直接拒绝；完整 payload 缺少 PID 时仅允许用唯一运行实例补全，多实例场景安全拒绝。时间轴身份同时绑定 PID 和媒体 ID，PID 切换会立即清除旧进度与 seek 锚点，同名稀疏新曲只有至少两个稳定字段一致或媒体 ID 相同时才可复用进度。
- 歌手、专辑和封面的异步补取现在绑定请求发起时的会话代次与 PID；旧、新汽水进程短暂并存时，旧 PID 的同名元数据回包不能覆盖当前进程。
- 汽水 Electron 辅助功能树在属性仍为开启时偶发退化为 2 个节点；控制失败路径现自动关闭再开启 `AXManualAccessibility` 并执行两次有界重扫，实测恢复到 363 个节点和唯一底部控制组。新增 `--qishui-control-diagnostic` 只读输出根节点、候选数量与安全门禁信息。
- 汽水主窗口完全关闭且 `AXWindows=count:0` 时，控制诊断会明确提示保留可见或最小化窗口；已验证汽水原生显示命令会抢占前台，且没有公开 URL、AppleScript、Dock 播放命令或外部本地 IPC，因此不会自动重开窗口，也不会回退到可能误控视频的系统媒体通道。
- 汽水控制能力改为随窗口生命周期自动更新：主窗口完全关闭后，展开态播放 / 暂停、上一首和下一首立即禁用并显示原因；窗口重新显示或保持最小化时自动恢复。每次实际控制前会再次执行轻量 `AXWindows` 探测，避免迟到通知或旧 UI 状态误发命令；Electron 暂时返回非标准窗口角色时保持严格语义扫描与 AX 树自修复，不误判为窗口已关闭。
- 设置“音乐 - 当前活动”新增可用控制诊断，复制诊断同时包含三项控制能力与禁用原因；Apple Music 控制按钮也按其定向适配器的实际 capability 独立启用。
- 汽水窗口存在但 Electron AX 树退化为非标准自引用节点时，不再误报“主窗口已关闭”；控制保持严格安全失败并提示辅助功能树暂不可用。
- 汽水控制可用性改为完整语义健康探针：只有后台确认唯一播放控制组时才启用三枚按钮；窗口存在但无候选或候选不唯一时进入 `controlTreeUnavailable`，按 3、6、12 秒退避自修复。`unknown` 不再默认放行，深层扫描与重试均在串行后台队列执行，且不随高频歌曲事件重复触发；已验证的唯一最小化窗口复用缓存健康状态，避免健康检查反复切换窗口。新增 `--qishui-control-availability` 只读验收命令。
- 移除仅用于本机验收的分布式媒体控制通知，发布包不暴露可由其他本地进程伪造的播放控制入口。
- 当前 112 项自动化回归测试通过；新增连续 10 首切歌、未适配视频前台隔离、快速播放暂停策略、Apple Music 统一快照准入与瞬时读取有界重试、双来源快速抖动、单窗口连续插值几何、动画代次、最小化窗口恢复、汽水窗口控制能力与唯一语义控件健康门禁回归。

### v0.1.1-alpha.2 音乐适配稳定化

- 修复汽水重启或长时间后台运行后播放按钮偶发无反应：控制前显式开启 Electron/Chromium 的 `AXManualAccessibility` 控件树，再执行唯一语义控件校验；该初始化不激活汽水、不切换前台 App，也不发送系统媒体命令。
- 修复播放按钮点击无反应：移除会吞掉点击的额外原生透明命中层，三个媒体控制统一恢复为标准 SwiftUI 按钮；展开 Body 从第一帧启用命中，并由 HostingView 接受 `first mouse`。窗口仍不能成为 Main，且保留 `.nonactivatingPanel`，不会把抖音等前台应用切走。
- 播放/暂停、上一首、下一首只在顶屿能够唯一确认汽水音乐进程内的语义播放控件时执行；无法确认时安全失败，不再使用会受 QuickTime 等系统媒体焦点影响的 MediaRemote 控制通道。
- 非激活的展开面板收到第一下 `mouseDown` 时先成为 Key，再把同一事件继续交给 SwiftUI 控件，避免抖音等其他 App 在前台时第一下点击只聚焦、不执行播放按钮。
- 实机复现并移除系统全局进度跳转：汽水暂停、抖音播放时，旧版点击汽水进度条会把抖音从 `15:34` 跳到 `16:40`；新版在汽水没有 client-targeted seek 前只显示可信进度，不再发送全局 seek。
- 产品播放状态不再采用普通系统 Now Playing 或控制中心诊断作为 fallback；歌曲、封面、播放态和进度只接受 `com.soda.music` 专属 client 流及汽水 PID 内 AX 数据。
- 汽水播放、暂停改为点击瞬间乐观反馈，后台汽水专属状态流负责确认；失败或超时仅回滚当前操作代次。
- 修复启动、展开、切歌和 metadata 回包竞争时的旧进度覆盖，优先采用新鲜 `elapsedTimeNow`，稳定播放由本地单调时间轴推进。
- 切歌元数据改为候选态统一提交：空 payload、临时媒体 ID、新歌名配旧歌手/专辑/时长/封面均只更新候选，不再通过 `snapshot()` 或位置刷新泄漏到界面；完整标题、歌手、专辑和封面一次发布。
- 封面补取加入曲目代次和 0.9 秒进程硬超时；A→B→C 快切时，A/B 的迟到回包与中间元数据不能覆盖 C，超时降级会清除所有已知旧字段。
- AX 只负责触发汽水控件和补充播放状态；同曲已有可信 MediaRemote 时间时，不再用旧 AX 进度覆盖。
- 抖音占用系统媒体焦点的实机回归证明，非公开 MediaRemote client 命令即使报告已派发，仍可能暂停系统当前视频而不是汽水；产品控制路径已禁用该通道，避免误控其他媒体。
- 播放、暂停、上一首和下一首改为直接触发汽水 PID 内经过主窗口、可见性、三按钮结构和底部播放器栏校验的唯一语义 AX 控件；`CFEqual + CFHash` 会合并 Electron 对同一底层控件的重复包装。
- 上一首和下一首接入独立操作代次驱动的短促反馈：固定按钮外框不位移、不旋转、不显示圆线，点击时仅增强底色并让图标中心回弹；连续点击每次都会重新触发，开启“减少动态效果”时只保留亮度闪烁。
- 实测汽水 2.9.1 底部进度轨道仅暴露只读 `AXGroup`，不存在 `AXSlider`、数值范围或可写 `AXValue`；新增 `QishuiProbe --settable-attributes` 诊断能力，继续阻断系统全局 seek，避免误控抖音或 QuickTime。
- 建立多音乐应用适配底座：新增应用描述、原子快照、时间轴、来源证明和绑定具体应用实例的控制能力协议；设置面板开始显示汽水音乐“已适配”和 Apple Music“计划接入”，`--music-adapters` 可只读输出注册状态。
- 当前 35 项自动化回归测试通过，覆盖权威时间轴、stream 差分、切歌元数据原子性、空 payload、同名媒体项、A→B→C 竞态、适配器硬超时、旧样本拒绝、连续操作、全局 seek 隔离、AX 进度隔离、适配器注册唯一性及实例绑定控制能力。

### GitHub 发布准备

- 统一公开预览版本为 `v0.1.0-alpha`，最低系统版本调整为 macOS 26.0，与当前 MediaRemote Adapter framework 一致。
- 新增公开 README、隐私说明、安全策略、贡献指南、第三方声明、Issue / PR 模板、CI、Release 候选构建脚本和发布检查清单。

### 当前候选功能

- 重排展开音乐面板的纵向结构：窗口模式按钮改为右上角悬浮层，不再独占 24pt 工具栏行；音乐内容使用完整 126pt 内高，标题与歌手区域为按钮保留 72pt 安全距离，底部媒体控制获得至少 12pt 留白。计时器和提醒继续沿用原有工具栏布局。
- 展开音乐面板的专辑封面放大至 88pt，封面与详情组合调整为 408pt；三枚媒体按钮改为与 224pt 进度轨共用中心线，消除控制区偏右。封面右下角根据歌曲来源 bundle identifier 显示 18pt 真实音乐 App 圆角方形角标，轻微跨出封面边缘；角标使用高质量插值和单层抗锯齿圆角遮罩，裁掉系统图标外缘并移除额外阴影与粗描边。未知来源安全降级，紧凑态不显示徽标。
- 简化音乐控制点击反馈：上一首 / 下一首移除图标方向位移和旋转等待环，播放 / 暂停也不再显示 pending 圆线；固定圆形按钮只做短促亮度与 0.96 按压缩放。连续切歌保留独立操作代次，旧操作结果或较早元数据回包不会影响最新操作，并尊重“减少动态效果”。
- 统一展开 / 收回动效时间轴：Header 几何只由 AppKit 窗口驱动，Body 几何只由 SwiftUI 遮罩驱动，禁用系统窗口默认缩放；移除独立 sleep 与末尾强制 snap，改用真实动画 completion，并将 Header、Body 内容改为可反向交叉淡化；同值活动发布不再重复启动窗口动画或短暂禁用展开态按钮。
- 新增正式 macOS App 图标：以 MacBook 摄像头刘海和三色状态波形构成深色圆角方块，并随安装包写入 `IslandAppIcon.icns`；核心符号针对 32px 小尺寸单独强化。
- 顶部 Header 改为贴屏的单一纯黑轮廓，移除重复刘海 Shape、描边与半透明边缘；整块 Header 统一点击命中，左侧封面区域也可正常展开。
- 新增低打扰悬浮交互：鼠标进入顶部岛 160ms 后自动展开，Header、8pt 间距和 Body 视为连续悬浮区域，移出 350ms 后恢复进入前形态；快速进出会取消过期任务，点击展开仍保持固定展开。
- 紧凑态移除播放 / 暂停图标，右侧统一为真实歌曲进度和专辑色三条波形；底部中央鼠标光圈确认为 Codex Computer Use 测试覆盖层，不属于 App 渲染。
- 收回顶部胶囊默认摄像头高度补偿 1pt，并让最终高度落在完整像素边界，减少摄像头模组下方多余黑边和透明底线；三条状态波形改为 Core Animation 播放动效，并从当前歌曲真实封面后台提取安全化专辑色，暂停或减少动态效果时保持静止。
- 恢复顶部 Header 与展开 Body 之间的 8pt 分隔，并将顶部可见黑色外壳、AppKit 窗口 frame 和 Body Shape 在同一模式切换周期内连续插值；展开和收回不再以宽度硬跳开始。
- 修复展开 / 收回瞬间横向偏移：禁用灵动岛 `NSHostingView` 按 SwiftUI 内容自动改写窗口尺寸，按目标模式同步插值 frame，并取消过期的连续点击重排任务；顶部窗口切换尺寸时始终保持屏幕中线不变。
- 重做展开 / 收回动效：取消整个 Body 窗口的透明度动画，改为纯黑 Shape 从顶部锚点展开和收缩；内容独立淡入淡出并由同一形变遮罩裁切，消除灰色闪块、空黑大面板和隐藏窗口吞点击。
- 修复展开态默认左偏：动画结束后按当前尺寸重新吸附屏幕中线，避免顶部窄岛残留切换前横坐标；音乐与计时器内容列及控制组改为明确居中。
- 新增 EventKit 日程事件源：用户可在设置的“日程”页分别授权日历和提醒事项；临近 10 分钟的定时日程、刚到期 5 分钟内的定时提醒进入已有低打扰事件队列，默认忽略全天日程和无具体时间的提醒。
- EventKit 读取采用后台快照查询，并通过设置代次和最终权限复核阻止关闭功能或撤权后的旧结果回灌；日历与提醒事项独立授权、独立降级，不影响汽水主活动。
- EventKit 事件按具体事项身份去重和取消，不同事项不会因来源相同而错误合并；最多保留 48 小时、512 条本地投递记录。
- 新增 `--eventkit-status` 只读权限诊断，不主动请求系统权限。
- 灵动岛改为单一主活动：正常启动默认显示汽水音乐，移除展开页的音乐 / 计时器 / 提醒固定标签以及设置中的默认功能选择。
- 新增本机 `IslandEvent` 入口和事件队列：普通事件短暂覆盖后恢复汽水，高优先级事件不会被普通消息覆盖，拖动、seek 和媒体控制确认期间不抢占用户交互。
- 普通事件只合并同一来源；不同应用来源分别排队。事件期间用户主动改变展开形态时，恢复内容但保留用户最新选择的形态。
- 媒体状态读取改为汽水专属客户端流：MediaRemote Adapter 使用 `stream-client com.soda.music` 持续订阅，并用 `get-client com.soda.music` 定向补取，不再让抖音、QuickTime 等系统当前媒体源决定岛内内容。
- 汽水 MediaRemote client 定向控制保留为诊断入口，不再用于产品按钮；抖音抢焦点实测发现它可能误控系统当前媒体。
- 语义化 AX 成为唯一产品控制通道：只绑定汽水 PID，并根据角色、结构和相邻控件特征选择唯一目标；唯一控件不可证明或辅助功能权限不可用时安全失败。
- 移除“实用模式 / 严格模式”、短暂激活汽水和全局媒体键运行路径。
- MediaRemote Adapter framework 更新为 `arm64` + `x86_64` Universal 二进制。
- 切歌元数据改为暂存后原子发布：新歌名出现但歌手或封面仍属于旧曲时，继续保留上一完整画面，待元数据一致后统一提交。
- 语义 AX 控制、播放回读和控件扫描均移出主线程；控制点击立即显示按压反馈，上一首和下一首使用独立操作代次，但不显示等待环。
- 模式点击冷却从 720ms 缩短到 80ms，控制后的模式抑制从 280ms 缩短到 60ms，改善连续点击响应。
- 收窄折叠、紧凑和展开尺寸；展开上下窗口以 3pt 重叠形成连续黑色轮廓，统一黑度、描边、圆角，并压缩计时器和提醒布局。
- 状态绿点改为白色微型波形，避免与 macOS 摄像头隐私指示灯混淆。
- 优化进度条单击跳转：单击与拖动使用独立手势，点击不再触发圆点缩放脉冲；单击采用更短的 seek 合并窗口，并保持即时本地预览和最终操作优先。
- 优化进度条拖动手感：拖动预览与时间标签同步，seek 和常规播放位置回读移出主线程，连续 seek 串行执行并带超时；命令确认前保持目标位置，避免松手卡顿和旧进度回弹。
- 修复播放/暂停后的时间回退：控制命令使用带操作代次和时间锚点的乐观播放状态，MediaRemote 仅用命令后的新样本确认，缓存样本只负责显示；暂停进度单调稳定，连续快速点击基于上一次目标状态反转。
- 设置面板显示固定的“汽水语义控件直控”策略，并可隐藏 / 显示音乐诊断信息。
- 音乐诊断模式新增 UI progress、elapsed、duration、canSeek、pending、封面字节数，并支持一键复制诊断信息。
- 优化切歌元数据稳定性：封面补齐回包必须匹配当前曲目，封面视图按曲目与封面身份刷新，减少旧封面残留。
- 修复音乐进度识别：AX 兜底不再把汽水窗口中的列表/旧歌时间误当成当前播放进度；主界面进度只使用 MediaRemote Adapter 的可信 elapsed/duration。
- 媒体焦点被其他播放器抢占时继续使用汽水客户端专属流，避免用其他 App 的 Now Playing 覆盖汽水状态。
- 修复点击/展开灵动岛时进度条视觉上从 0 重新开始的问题：非拖动状态下进度条禁用隐式宽度动画，直接显示当前可信进度。
- 修复点击岛时进度被 fallback 状态刷回 0 的问题：切换折叠/展开前先刷新当前可信播放状态，并阻止无 `elapsedTime/duration` 的 AX/placeholder 状态覆盖已有 MediaRemote 进度。
- 修复点击岛后进度显示成开头几秒的问题：折叠/展开前改为同步刷新一次 MediaRemote Adapter 播放位置，并在播放中每约 2 秒校准一次真实 elapsed/duration。
- 优化汽水 AX 监听：不再每 2 秒强制重扫可访问性树，减少观察节点和通知类型，降低同步抖动与后台开销。
- 进度跳转在不能确认汽水为可跳转目标时保持阻断，避免 seek 误控视频播放器；汽水专属定向 seek 仍是后续底层任务。
- 修复抖音/Chrome 视频页并行时主 App 高 CPU、灵动岛点击无响应的问题：常规 UI tick 不再同步拉取 MediaRemote/AX，音乐状态发布改为事件驱动加低频进度刷新。
- 启动时不再自动打开设置窗口；设置/校准窗口关闭后释放 SwiftUI 内容，避免隐藏窗口继续重排拖慢灵动岛。
- 修复布局校准自触发循环：屏幕信息未变化时不再重复发布 `currentDisplayName`、`notchWidth` 和 `topBandHeight`，主 App 稳态 CPU 恢复到低占用。
- 新增产品需求文档、路线图、QA 检查清单。
- Git 忽略第三方 App 解包产生的本地 JavaScript 调查文件，避免把专有产物纳入产品仓库。
