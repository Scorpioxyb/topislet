# v0.1.2 GitHub 发布检查清单

目标 Tag：`v0.1.2-alpha.1`

目标 App 版本：`0.1.2 (19)`

## P0 代码与实机

- [x] 建立 v0.1.2 范围、质量指标和独立发布清单。
- [x] 新增三来源定向、Apple Music 电台占位、多显示器稳定绑定、登录项和布局恢复回归，完整 182 项自动化测试通过。
- [x] CI 增加汽水控制不得改写 `AXMinimized` 的静态门禁。
- [x] Apple Music 普通歌曲与电台安装版通过：真实标题、歌手、封面、暂停冻结、恢复和下一首控制均收敛；电台目录封面实测 `758ms`。
- [ ] 安装版三来源前台覆盖、退出回退和控制目标交叉实测通过。
- [ ] 汽水可见与最小化状态各完成上一首、播放 / 暂停、下一首回归。
- [ ] 汽水最小化控制 90 次，弹窗、前台切换和误控均为 0。
- [ ] 汽水主窗口关闭时三项控制安全禁用，重新显示或最小化后自动恢复。
- [ ] 抖音、QuickTime、Safari 和 Chrome 媒体冲突回归通过。
- [ ] 三来源累计 60 次切换无旧控制反馈或混合元数据。
- [ ] 展开、收回、外部点击和连续反向悬停通过视觉验收。
- [x] `swift test`、Release 构建和窗口锚定脚本通过；双屏实测悬停响应 `55ms`、最终安装版移出收回响应 `404ms`，中心无迁屏漂移。

## P1 产品完整性

- [x] 开机启动代码、自动化、安装版注册 / 注销和系统状态同步通过；`sfltool dumpbtm` 确认 `/Applications/顶屿.app` 为 `enabled, allowed`，最终保持开启。下一次正常重新登录时补充观察实际启动时机。
- [x] 设置中可显示 / 隐藏岛、带确认地重置当前屏幕布局，并集中显示辅助功能、Apple Music 自动化、日历和提醒事项状态及恢复入口；182 项测试与安装版只读视觉验收通过。
- [ ] 插拔外接屏或改变排列后无需重启。
- [ ] 系统减少动态效果开启和关闭时均可用。
- [ ] 两小时长稳测试无持续 CPU、内存、线程或子进程增长。

## 发布

- [ ] `Packaging/Info.plist` 更新为 `0.1.2 (19)`。
- [ ] README、CHANGELOG、ROADMAP、已知限制和支持矩阵同步。
- [ ] 从干净提交构建 `TopIslet-v0.1.2-arm64.dmg`。
- [ ] DMG、App 签名、arm64、macOS 26.0 最低版本和 SHA-256 验证通过。
- [ ] 两条 GitHub CI 通过，工作区干净。
- [ ] 创建精确 Tag `v0.1.2-alpha.1` 并上传 GitHub Release。
- [ ] 从 GitHub Release 重新下载、安装并完成发布后验收。

## 当前分发限制

Apple Developer 证书尚未作为本版本前置条件。发布页必须明确说明 App 使用 ad-hoc 签名、未公证，首次运行可能出现 Gatekeeper 提示。
