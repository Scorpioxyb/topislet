# 问题看板

## P0 - MVP 阻断

| 状态 | 问题 | 说明 | 负责人 | 下一步 |
| --- | --- | --- | --- | --- |
| 已验收 | 抖音抢焦点时汽水持续同步 | 2026-07-16 独立抖音 App `7.8.0` 前台实测：视频从 `24:40` 连续到 `24:49`；汽水在最小化状态下暂停/恢复、下一首、上一首均成功，抖音始终前台且零误控 | macOS 架构师 / QA | 后续系统或汽水版本更新时重复同一回归 |
| 已验收 | 汽水专属安全控制 | 汽水重启后自动启用 Electron 的 `AXManualAccessibility` 控件树，再只按压汽水唯一语义控件 | macOS 架构师 / 开发工程师 | build 17 实测播放/暂停、下一首、上一首均命中汽水；QuickTime 前台、播放开关和时间线零变化 |
| 已验收 | 汽水控件树健康门禁 | 只有完整扫描确认唯一播放控制组才启用三枚按钮；2 节点自引用、0 候选和多候选均明确禁用并后台退避恢复 | macOS 架构师 / QA | 2 节点退化、安全禁用、自动恢复 `available`、暂停 / 恢复播放和 110 项回归均通过 |
| 已验收 | 进度显示 / 暂停后时间回退 | 可信时间线、防旧回包和暂停冻结已通过自动化与实机回归；进度保持只读，不再保留全局 seek | 开发工程师 / QA | 继续观察长时间播放与系统更新兼容性 |
| 已验收 | 切歌后歌名、歌手、封面原子更新 | 已建立已提交状态 / 候选状态门控，空 payload、临时媒体 ID、旧字段和迟到回包不会泄漏；封面读取有硬超时 | macOS 架构师 / QA | 自动化 31 项、14 次实机切歌与产品负责人视觉验收均通过；合并 PR #4 后进入音乐适配发布收尾 |
| 已验收 | 浏览器视频并存 | Chrome 与 Safari 带声音视频分别成为系统媒体焦点时，汽水专属同步及三种控制均通过；浏览器持续播放、保持前台且鼠标零位移 | macOS 架构师 / QA | 后续 macOS、Safari、Chrome 或汽水版本更新时重复同一回归 |

## P1 - 体验优化

| 状态 | 问题 | 说明 | 负责人 | 下一步 |
| --- | --- | --- | --- | --- |
| 已验收 | 单窗口连续轮廓 | 顶部胶囊和展开主体已合并到同一 `NSPanel`；本机 WindowServer 验证三种状态始终只有 1 个窗口，紧凑态 `377x34`、展开态 `460x190`，中心和顶边不漂移 | UI/UX / 开发工程师 | 后续 UI 尺寸变更时重复同一结构与视觉回归 |
| 已验收 | 可中断形变动画 | 单窗口 frame 在紧凑态与展开态之间连续插值，窗口整体长出并带动内容形变；顶部与水平中心固定，且只有最新动画代次可落最终 frame | UI/UX / 开发工程师 | 自动化和产品负责人本机视觉验收均通过；后续不得改回透明画布瞬时切换 |
| 已完成 | 进度条只读反馈 | 只显示汽水专属可信进度；系统全局跳转已移除，AX 只读轨道不会冒充可拖动能力 | UI/UX / macOS 架构师 | 汽水出现定向接口后再单独设计跳转交互 |
| 部分完成 | 设置面板产品化 | 已显示音乐支持等级、运行状态、辅助功能 / 自动化权限、连接结果、响应耗时和最近同步状态 | UI/UX 设计师 | 继续补充开机启动、重置布局和权限撤销后的引导 |
| 已完成 | 状态诊断可视化 | 音乐页显示来源、同步状态、更新时间、连接错误级别、UI 时间轴、seek/pending 和封面字节数，并支持复制 | 开发工程师 | 后续按真实用户反馈扩展，不增加高频诊断轮询 |
| 已完成 | 多音乐应用适配底座 | 已建立应用描述、原子快照、时间轴、来源证明和实例绑定控制协议；汽水为主适配，Apple Music 与网易云音乐为 Alpha 支持 | macOS 架构师 / 开发工程师 | 三来源路由、实例绑定控制和退出回退自动化已通过；下一步扩大网易云实机样本并探测 QQ 音乐 |
| Alpha 已接入 / Beta 候选 | 网易云音乐适配 | Bundle ID 与 PID 专属状态、封面、进度和三项原生语义控制已接入；新 PID 重绑、漏事件自愈、同封面切歌、媒体焦点隔离、切歌占位抑制及真实“私人漫游”均已通过 | macOS 架构师 / 开发工程师 / QA | 当前版本本机验收已完成；补齐不同网易云版本或第二台设备样本后再升级 Beta，定向 seek 仍保持阻断 |
| 已验收 | Apple Music 封面切换与恢复 | 同艺人同专辑直接复用已验证封面；跨专辑严格读取新封面；原生缺失、目录失败和空状态按当前歌曲身份有界恢复，旧任务不能覆盖新歌曲 | macOS 架构师 / 开发工程师 / QA | 空态、同专辑、跨专辑、电台及连续三次切歌真机验收通过；继续收集不同账号、弱网和设备样本 |
| 已验收 | Apple Music 电台瞬时读取失败回退汽水 | 同一 Music 进程切歌返回 `.degraded` / `409` 时保留最近可信快照并执行四次有界短重试；退出、PID 变化和权限错误不保留 | macOS 架构师 / 开发工程师 / QA | ATEEZ 电台实测 `683ms` 发布新歌、`1156ms` 补齐封面且未切回汽水；后续继续扩大电台样本 |
| 已验收 | Apple Music 控制确认重复读取 | 控制后的时间线确认会等待已在途的权威元数据读取；所有结果统一走快照准入，不再同时发送 `metadata` 与 `timeline` Apple Event | macOS 架构师 / 开发工程师 / QA | `Bad for Business` 暂停实测仅保留一次元数据读取，按钮 `+4ms` 乐观发布，进度冻结无回退 |
| 已完成 / 上游受阻 | 汽水无 AX 窗口时安全控制拒绝 | 汽水 2.9.1 没有外部播放 URL、AppleScript、Dock 命令或本地 IPC；顶屿现会自动识别主窗口关闭并禁用三项控制，重新显示或最小化窗口后自动恢复 | 产品策略师 / macOS 架构师 | 产品侧防误控已完成；继续等待汽水提供无窗口定向控制接口，不得回退系统媒体键或私有 client 控制 |

## P2 - 后续能力

| 状态 | 问题 | 说明 | 负责人 | 下一步 |
| --- | --- | --- | --- | --- |
| 部分完成 | 计时器正式化 | 已移除固定入口，改为从菜单主动启动并在到时后发送高优先级事件 | 产品策略师 / 开发工程师 | 接入持久化与系统通知权限 |
| 待验收 | 日历 / 提醒事项事件源 | 已接入 EventKit，分别授权后支持临近定时日程和刚到期定时提醒；后台查询、代次门禁和事项级去重已完成 | macOS 架构师 / QA | 在设置中授权后做真实事件、撤权、关闭开关和大日历库流畅度验收 |
| 部分完成 | 通知提醒能力 | 已建立本机事件入口、跨来源队列和交互锁，并接入 EventKit；尚未适配第三方消息应用 | 产品策略师 / 开发工程师 | EventKit 实机验收后，再按应用价值逐个适配消息来源 |
| 上游受阻 | 汽水专属定向 seek | 当前进度只读；系统全局 seek 已移除，player-path 私有 API 也会误控 QuickTime | macOS 架构师 | 等待汽水公开接口、URL contract 或唯一可写 AX 控件后重启验证 |

## 已解决

| 问题 | 结果 | 相关提交 |
| --- | --- | --- |
| 主 App 高 CPU | 修复布局校准自触发循环、降低 AX 监听开销 | `58c440f`, `44ac907` |
| 双窗口展开收回不同步 | 顶部与主体合并为一个窗口，统一中心、顶边、悬停、外部点击和动画代次 | 待本次提交 |
| 汽水最小化后控制树不完整 | 对唯一标准窗口后台临时取消最小化，完成唯一语义控制后恢复；不激活应用、不移动鼠标，抖音前台实测三种控制通过 | 待本次提交 |
| CI 未执行自动化测试 | CI 增加 `swift test`，Release 候选增加精确 tag、干净工作区与全量回归门禁 | 待本次提交 |
| Documents 文件提供器污染开发包校验 | 开发包在最终安装位置清理扩展属性并严格验签；正式 DMG 继续在系统临时目录构建 | 待本次提交 |
| AX 误识别进度 | AX 不再参与主进度计算 | `522e752` |
| 进度条点击归零动画 | 禁用非拖动状态隐式宽度动画 | `60a4d6d` |
| 点击岛进度被 fallback 刷回 0 | 点击前刷新可信播放位置并加防回退闸门 | `c11ce8b`, `6e6ecf6` |
| 全局媒体键和聚焦兜底风险 | 已移除全局媒体键、短暂激活汽水和不可靠的 client 产品控制，改为汽水唯一语义 AX 直控 | 待本次提交 |
| 系统当前媒体源覆盖汽水 | 数据读取改为汽水专属 client stream；抖音、QuickTime、Chrome 与 Safari 动态并存回归均已通过 | 待本次提交 |
| 控制策略与诊断开关 | 设置面板固定显示汽水语义控件直控，并保留音乐诊断显示开关 | 待本次提交 |
| 暂停确认后时间退回旧值 | 播放控制改为带时间锚点的乐观状态机；暂停冻结、恢复推进、快速双击反转均已实机验证 | 待本次提交 |
| MediaRemote stream 退出后遗留后台进程 | stream 使用进程代次门禁、可取消单飞重启和 TERM/KILL 有界回收；Ctrl+C 实测主进程与子进程同时退出 | 待本次提交 |
| 播放 / 暂停时歌手封面短暂清空 | 汽水随机 contentItemIdentifier 不再作为稳定歌曲身份；同曲控制事件保留完整元数据，实机 stream 回归通过 | 待本次提交 |
| 汽水 AX 属性开启但控件树退化 | 异常小树路径自动重建 AXManualAccessibility；实测从 2 节点恢复到 363 节点和唯一安全控制组 | 待本次提交 |
| 汽水主窗口关闭后按钮仍可点击 | 监听窗口创建、销毁、最小化和恢复事件，并用轻量 AXWindows 探针自动更新三项控制能力；执行前再次探测，避免迟到事件误发命令 | 待本次提交 |
| 汽水窗口存在但控件树退化时按钮仍显示可用 | 控制能力改用与真实控制一致的唯一语义控件健康探针；异常态禁用并按 3/6/12 秒后台重试，`unknown` 不再放行 | 待本次提交 |
| 汽水重启后旧 PID 事件迟到 | PID 纳入稳定身份；退出/切换后的旧 PID 短期退役，同名稀疏事件不再刷新播放证据 | 待本次提交 |
| 汽水完全退出后残留 payload 恢复旧歌 | 无运行 PID 时身份校验直接拒绝；缺失 PID 仅从唯一实例补全；PID/媒体 ID 纳入时间轴身份，切换时清除旧锚点 | 待本次提交 |
| 旧 PID 同名元数据迟到覆盖当前封面 | 异步 metadata 请求绑定 session generation 与 expected PID，并拒绝退役或非当前 PID 回包 | 待本次提交 |
| Apple Music 电台切歌后约 30 秒才恢复 | 同进程瞬时 `.degraded` 快照不再立即清空来源；改用四次有界短重试，真实退出和权限失败仍立即降级 | 待本次提交 |
| Apple Music 控制后并发重复读取 | 控制确认优先等待在途元数据读取；元数据、时间线和封面结果统一准入，迟到结果不能取消仍需执行的短重试 | 待本次提交 |
