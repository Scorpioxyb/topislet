# v0.1.2 最终候选两小时长稳验收

日期：2026-07-26

## 结论

提交 `813f841f0010dbfc1aa2a97171d7c96c302c9953` 对应的 v0.1.2 最终候选完成完整两小时连续监测：实际观察 `7200.02s`，每 5 秒采样，共 1438 个样本，摘要 `violations` 为空。RSS、physical footprint、CPU、线程、直接子进程、窗口和进程生命周期全部通过既定门禁，101 分钟旧版观察暴露的内存阻断项正式关闭。

本轮安装版 Info.plist 仍显示 `0.1.1 (18)`；`0.1.2 (19)` 版本号升级属于后续发布包装步骤，仍保留在发布清单中。本记录只关闭两小时长稳门禁，不提前宣称整个 v0.1.2 已发布就绪。

## 环境与方法

- 分支：`codex/v0.1.2-stabilization`
- 候选提交：`813f841f0010dbfc1aa2a97171d7c96c302c9953`
- 安装路径：`/Applications/顶屿.app`
- 系统：macOS `26.5.2 (25F84)`，Apple Silicon arm64
- 主进程：PID `14893`，测试期间未重启或更换
- 实际负载：汽水音乐自然连续播放，专属状态流直接携带封面；保持网易云与汽水两个预期状态流，不额外启动高频调试流
- 测试期间冻结候选 Release 与源码，不构建、不安装、不改代码

监测命令：

```bash
swift Scripts/monitor-long-run.swift \
  --pid 14893 \
  --duration 7200 \
  --interval 5 \
  --output .build/qa/topislet-final-candidate-2h-2026-07-26.json
```

## 资源门禁结果

| 指标 | 基线 | 结束 | 峰值 | 标准 | 结果 |
| --- | ---: | ---: | ---: | --- | --- |
| RSS | 71,548,928 B | 72,466,432 B（`+1.28%`） | 72,777,728 B（`+1.72%`） | 结束增长不超过 15% | 通过 |
| Physical footprint | 25,641,896 B | 28,836,800 B（`+12.46%`） | 28,836,800 B（`+12.46%`） | 结束增长不超过 15% | 通过 |
| CPU | — | 平均 `0.0303%` | P95 `0.0369%`，最大 `0.0602%` | 不连续 30 秒高于 5% | 通过 |
| 线程 | 5 | 5 | 7 | 不持续增长 | 通过 |
| 直接子进程 | 2 | 2 | 3；最长瞬时增长 `10.03s` | 回归基线且无持续增长 | 通过 |
| 可见窗口 | 1 | 1 | 1 | 始终单窗口 | 通过 |
| 主进程 | 存活 | 存活 | PID 未变化 | 全程存活 | 通过 |

监测脚本最终记录：

- `completed=true`
- `observedDurationSeconds=7200.0199810266495`
- `sampleCount=1438`
- `maximumSustainedCPUAbove5Seconds=0`
- `maximumSustainedThreadGrowthSeconds=0`
- `violations=[]`

## 结束后健康检查

- `codesign --verify --deep --strict --verbose=2 /Applications/顶屿.app`：通过，安装版满足 Designated Requirement。
- 安装版主可执行文件：Mach-O 64-bit arm64。
- 主进程 PID 仍为 `14893`；结束时仅保留汽水与网易云两个预期 MediaRemote 状态流。
- 汽水专属读取：`verifiedQishuiSource=true`、`isPlaying=true`，曲目、时长、进度和 `9065 bytes` 封面数据均有效。
- `swift test`：198 项通过。
- `Scripts/verify-qishui-control-safety.sh`：通过。
- 记录更新前 Git 工作区干净；长稳期间未产生源码变化。
- 本轮未使用 Computer Use、OCR、屏幕录制或合成鼠标事件，没有鼠标覆盖层或控制会话残留。

## 原始证据

- `.build/qa/topislet-final-candidate-2h-2026-07-26.json`
  - SHA-256：`ffa5aae036b83d039b99b70db1507cf84a7c5b34f7692659de3e1ad66b85ef56`
- `.build/qa/topislet-final-candidate-2h-2026-07-26.csv`
  - SHA-256：`0edba02eda9932634cd62af7a8d7c97f17cd0c9c9878111af6b01052c0bc091e`

`.build/qa` 原始采样文件保持为本机 QA 证据，不纳入 Git；仓库提交本验收摘要与发布清单状态。

## 后续

两小时长稳阻断项关闭。项目继续处理发布清单中尚未完成的安装版三来源交叉实测、汽水 90 次控制、60 次来源切换、视觉确认、版本号升级和 GitHub 发布包装，不以本轮资源通过替代这些独立验收项。
