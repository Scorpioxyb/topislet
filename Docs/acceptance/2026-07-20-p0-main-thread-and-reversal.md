# P0 主线程与反向动画验收

日期：2026-07-20

## 范围

- 保持新增音乐应用适配冻结。
- 为主岛专辑封面增加 12 项 LRU 图像缓存，避免进度发布触发重复 `NSImage` 构造。
- ticker 直接在其主 RunLoop 回调中执行，不再每 0.5 秒创建额外主线程 Task。
- ticker 的同一轮更新只执行一次完整音乐来源选择。
- 将顶部内容淡入淡出与岛宽度形变拆分到独立动画事务。
- 增加“收回途中重新进入”的实机反向动画门禁。

## 自动化结果

- `swift test`：147 项通过，0 项失败。
- 新增 3 项封面缓存测试：同数据单次解码、LRU 淘汰、失败数据不缓存。
- `bash -n Scripts/package-app.sh Scripts/build-release.sh`：通过。
- `git diff --check`：通过。

## 实机动画结果

`swift Scripts/verify-island-window-animation.swift`：

- 窗口数量：1。
- 紧凑尺寸：377x34。
- 展开尺寸：460x190。
- 悬停开始响应：50ms。
- 悬停保持：12.0 秒。
- 收回动画开始后重新进入，可中断收回并稳定恢复展开态。
- 全过程尺寸不越过紧凑态与展开态边界，顶部与中心锚定通过。

测试脚本同时修复了物理指针过早重新关联导致的假失败：测试期间暂时解除物理指针关联，任何成功或失败路径都会恢复测试前位置并重新关联。

## 主线程采样

使用 Release 安装包分别进行 5 秒、1ms 间隔系统栈采样：

- 修改前主线程样本 4190，RunLoop 消息等待样本 4062，等待占比约 96.9%。
- 修改后主线程样本 4298，RunLoop 消息等待样本 4247，等待占比约 98.8%。
- 修改后未采到 `NSImage(data:)` 重复解码，也未发现同步 AX、Apple Event 或 MediaRemote 阻塞主线程。
- 修改后 5 次空闲 CPU 采样为 1.0%、1.1%、1.1%、0.7%、0.4%，RSS 稳定在 64432 KB。

样本时长和系统负载有限，数据仅作为同机回归证据，不代表所有设备上的固定性能提升。

## 生命周期

- 旧主进程停止后残留主进程和状态流数量为 0。
- 新版本启动后只有汽水、网易云两个预期状态流，未重复创建。
- `/Applications/顶屿.app` 使用 Release 配置安装，严格代码签名验证通过。

## 结论

主线程阻塞审计、封面重复解码优化和状态流退出回收通过本批门禁。内容动画已经通过结构与反向操作验证，仍保留产品负责人视觉手感确认。
