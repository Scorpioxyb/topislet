# v0.1.2 减少动态效果验收

日期：2026-07-25

## 范围

- 跟随 macOS“减少动态效果”系统设置，不增加顶屿私有开关。
- 运行中监听系统设置变化，无需重启顶屿。
- 开启后取消紧凑、展开和收回的窗口几何动画，保留短透明度反馈。
- 关闭后保留原有展开 `240ms`、收回 `180ms` 动画。
- 常规设置页只读显示系统当前状态。

## 自动化与构建

- 完整 `swift test`：184 项通过，0 项失败。
- 新增两项回归：开启时窗口动画时长为 0 且 SwiftUI 几何动画为空；关闭时保留原动画时长和几何动画。
- Release 构建和 `/Applications/顶屿.app` 安装通过；可执行文件为 arm64，ad-hoc 严格签名验证通过。

## 安装版实测

- [x] 系统设置关闭“减少动态效果”时，展开和收回保持原有连续动画。恢复原设置后的安装版窗口门禁实测：单窗口，紧凑 `377×34pt`、展开 `460×190pt`，悬停响应 `75ms`、移出收回响应 `471ms`，12 秒保持和收回途中反向展开通过。
- [x] 系统设置开启“减少动态效果”时，窗口直接稳定到目标 frame，不出现缩放、偏心或迁屏。实测悬停响应 `60ms`、移出收回响应 `428ms`，采样只出现起点或终点尺寸，无几何中间帧。
- [x] 在顶屿运行期间切换两种状态，岛的行为无需重启即可更新。
- [x] 验收后已恢复用户原系统设置为关闭，并关闭系统设置窗口、重置 Computer Use 会话。

## 结论

代码、184 项自动化门禁、Release 安装和“减少动态效果”开关双状态实测全部通过，发布清单对应项目已关闭。
