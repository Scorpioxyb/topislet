# 汽水切歌元数据原子性回归

日期：2026-07-13

## 自动化

- `swift test -debug-info-format none`
- 31 项通过，0 失败。
- 覆盖空 payload、临时媒体 ID、单个旧字段、同名媒体项、A→B→C 迟到回包、超时降级和 0.9 秒适配器进程硬超时。

## 实机

- 安装位置：`/Applications/顶屿.app`
- 版本：`0.1.1 (17)`，临时候选构建。
- 首轮执行 5 次下一首和 5 次上一首；强化门控后再执行 2 次下一首和 2 次上一首。
- 顶屿实际界面及 `--adapter-watch` 发布回调均显示完整的歌名、歌手、专辑和非零封面字节，未出现新歌名配旧歌手、旧专辑或旧封面。
- `codesign --verify --deep --strict --verbose=2 /Applications/顶屿.app` 通过。

## 验收结论

- 产品负责人已观察实际切歌动效和停留时长，并确认“保持上一首完整画面，再一次切到新曲”的节奏通过验收。
