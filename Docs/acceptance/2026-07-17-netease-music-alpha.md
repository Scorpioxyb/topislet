# 网易云音乐 Alpha 验收记录

日期：2026-07-17

## 环境

- 设备：MacBook Air (`Mac17,4`)，Apple M5，24 GB
- 系统：macOS 26.5.2 (`25F84`)
- 网易云音乐：3.1.8 (`3368`)
- Bundle ID：`com.netease.163music`
- 验收进程 PID：`62598`

## 已通过

- `--netease-music-status` 只接受当前网易云 Bundle ID 与运行 PID，返回真实歌名、歌手、封面、进度、时长和播放状态。
- 单次专属状态读取延迟为 `42ms`，当前封面为 `10261 bytes`。
- 播放 / 暂停、上一首和下一首均通过网易云 PID 内原生“控制”菜单执行，不发送全局媒体键、不激活应用、不移动鼠标。
- 切歌期间的“旧歌名 / 旧封面 + 新时长”混合快照不会进入 UI；完整歌曲信息与封面原子发布。
- 下一首从《玻璃》切到《决战奥林 (Live)》在 `0.761s` 发布完整快照；上一首切回《玻璃》在 `0.758s` 发布完整快照。
- `swift test` 共 123 项通过；新增覆盖 Bundle ID / PID 拒绝、易变 UUID、暂停时间线、混合快照、完整指纹、三来源优先级、前台切换和退出回退。
- `/Applications/顶屿.app` 已重新打包安装，严格签名验证通过。
- Computer Use GUI 验收通过：紧凑岛显示网易云歌曲；展开态显示真实封面、歌名 / 歌手、进度、网易云来源 Logo 和三枚控制按钮。
- 展开态暂停后按钮立即变为播放图标，`2.2s` 后进度仍冻结在 `1:34`；恢复后按钮回到暂停图标并继续推进。
- 下一首确认期间保留《Someone in the crowd》完整画面，随后一次切换为《飞1999》、`99 God`、新封面和 `0:13 / 1:39`，未出现混合状态。
- 设置音乐页显示网易云“Alpha 支持 / 已连接”，运行中、已授权、连接正常，可用控制为播放暂停、上一首、下一首；当前活动来源为“网易云 PID 语义 AX”。
- GUI 验收结束后已关闭设置窗口并重置 Computer Use 会话，没有遗留鼠标覆盖层。

## 待补验

- 同专辑、相同封面的连续切歌需验证双快照确认延迟和原子性。
- 网易云电台、应用完整退出后的默认岛回退，以及抖音 / QuickTime / 浏览器视频并存仍需实机回归。
- 网易云当前没有经过来源证明的定向 seek，进度条必须保持只读。
