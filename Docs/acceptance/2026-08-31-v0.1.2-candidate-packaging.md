# v0.1.2 候选版本打包验收

日期：2026-08-31

## 本轮范围

- 将 App 版本推进到 `0.1.2 (19)`。
- 验证进度条点击 / 拖动手势改造后的编译、测试和 Release 打包链路。
- 验证单一音乐来源准入相关代码没有引入额外媒体控制进程。

## 结果

- `swift test`：205 项通过。
- `swift build -c release --arch arm64`：通过。
- `Scripts/verify-qishui-control-safety.sh`：通过。
- `Scripts/package-app.sh`：通过；`/Applications/顶屿.app` 包内为 `0.1.2 (19)`，主二进制为 arm64。
- `/Applications/顶屿.app`：代码签名验证通过，Designated Requirement 满足。
- 运行时只有 1 个顶屿主进程和 2 个只读状态观察流：`com.soda.music`、`com.netease.163music`；观察流不会播放音频，也不会同时向岛内发布多个来源。
- `VERSION=0.1.2 BUILD_NUMBER=19 bash Scripts/build-release.sh`：通过。
- `Scripts/verify-release-dmg.sh .build/release-artifacts/TopIslet-v0.1.2-arm64.dmg`：通过。
- DMG 在本地干净工作区构建并验证；当前候选标签为 `v0.1.2-alpha.1`，DMG SHA-256：`092fc6b13c36bed165da0b9195253b056df8bba107bef1c88ba91b9f277ce48f`。

## 当前边界

本记录覆盖候选包、签名、进程数量和自动化回归。当前汽水处于暂停态且主窗口关闭，三项语义控制按安全策略禁用；汽水实际播放时的点击 / 拖动跳转、三来源交叉播放和视频媒体并存仍需在具备真实播放状态后完成手工验收，不能由本轮静态检查替代。
