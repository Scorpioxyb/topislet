# P0 悬停性能与窗口锚定验收

日期：2026-07-19

## 范围

- 高频鼠标移动事件合并，只向主线程投递每批最新位置。
- 本地安装默认改用 Release 构建。
- 修复 AppKit 窗口尺寸动画中间帧的 1pt 水平中心漂移。
- 不修改汽水音乐、Apple Music、网易云音乐的来源选择和控制语义。

## 自动化结果

- `swift test`：144 项通过，0 项失败。
- 新增 3 项事件合并测试：最新位置覆盖、下一批重新调度、取消清理。
- `bash -n Scripts/package-app.sh Scripts/build-release.sh`：通过。
- `git diff --check`：通过。

## 实机动画结果

`swift Scripts/verify-island-window-animation.swift`：

- 窗口数量：1。
- 紧凑尺寸：377x34。
- 展开尺寸：460x190。
- 悬停开始响应：60ms，低于 120ms 门禁。
- 悬停展开保持：12.0 秒。
- 展开与收回期间顶部、水平中心无超限漂移。

修复前同一脚本连续两次测得最大水平中心漂移 1.00pt；诊断帧预期中心 855.50pt，实际最低 854.50pt。修复后重新按当前窗口宽度约束动画原点，脚本通过。

## 安装与运行结果

- `/Applications/顶屿.app` 已使用 Release 配置重新安装。
- 严格代码签名验证通过，主程序为 arm64 Mach-O。
- 旧主进程退出后两个状态流子进程均回收；新版本启动后主进程为 4 个线程，只存在网易云和汽水两个预期子进程。
- 新版本 5 次空闲采样：CPU 1.5%、1.5%、1.2%、1.2%、0.7%；RSS 稳定在 74144 KB。

旧 Debug 安装版的审计样本约为 CPU 1.1%-4.2%、RSS 100832 KB。两组样本的运行时长和媒体负载不同，因此只作为后续回归基线，不单独用于归因。

## 结论

本批 P0 通过自动化、Release 安装和实机动画门禁。下一批继续审计动画期间的主线程工作、状态发布频率和封面解码，且不新增音乐应用适配。
