# v0.1.2 显示器热变化验收

日期：2026-07-25

## 范围

- 顶屿运行中响应显示器 frame、缩放、排列和连接状态变化，无需重启。
- 显示参数变化立即作废旧窗口动画，并执行一次即时刷新和两次有界稳定化复核。
- 相同物理显示器重连后使用 UUID 保持稳定身份；排列变化复用原校准，缩放档位独立保存。
- 绑定屏仍存在时不受 `NSScreen.main` 变化影响；绑定屏消失时优先回退到带摄像头模组的屏幕。

## 自动化

- 完整 `swift test`：191 项通过，0 项失败。
- 新增 7 项回归：排列变化使用新 frame、显示变化作废旧动画、稳定化复核时序、物理 UUID 重连身份、校准身份区分排列与缩放、旧编号校准迁移、已有 UUID 校准优先。
- `git diff --check`：通过。

## 安装版热变化实测

安装版在 PID `69084` 运行期间完成以下切换：

1. 默认 `1710×1107pt`：摄像头锚点 `855.5pt`，紧凑窗口 `377×34pt`。
2. 临时切换至 `1440×932pt`：PID 未变化；摄像头锚点自动变为 `720pt`，紧凑窗口自动变为 `348×30pt`。
3. 该档位动画门禁通过：悬停响应 `50ms`、移出收回响应 `416ms`，12 秒保持和收回途中反向展开通过。
4. 恢复 `1710×1107（默认）`：PID 仍为 `69084`；锚点恢复 `855.5pt`，紧凑窗口恢复 `377×34pt`。
5. 恢复后动画门禁再次通过：悬停响应 `40ms`、移出收回响应 `406ms`。

全程没有重启顶屿、没有第二个窗口、没有横向补跳。系统显示设置已恢复原状，系统设置窗口已关闭，Computer Use 会话已重置。

## 外接屏排列热更新实测

安装版在 PID `71088` 运行期间连接外接显示器 `H27E6`，并完成一次可恢复的排列变化：

1. 双屏基线：内建屏 frame 为 `{{0, 0}, {1710, 1107}}`，外接屏 frame 为 `{{1710, -333}, {2560, 1440}}`；顶屿窗口保持在内建屏 `(667, 0, 377, 34)`。
2. 将外接屏竖直位置临时下移 `120pt` 后，其 frame 自动更新为 `{{1710, -453}, {2560, 1440}}`；物理 UUID 仍为 `28556D51-AE91-4A85-87A1-6315582802DA`。
3. 顶屿 PID 未变化，窗口仍为内建屏单窗口 `(667, 0, 377, 34)`，没有迁屏、补跳或第二个窗口。
4. 变化态动画门禁通过：紧凑态 `377×34pt`、展开态 `460×190pt`、悬停响应 `40ms`、移出收回 `418ms`，12 秒保持与收回途中反向展开通过。
5. 外接屏已恢复原 frame `{{1710, -333}, {2560, 1440}}`；恢复后 PID 仍为 `71088`，顶屿窗口仍为 `(667, 0, 377, 34)`。
6. 恢复后动画门禁再次通过：悬停响应 `60ms`、移出收回 `418ms`，单窗口、中心、顶边、连续插值、12 秒保持和反向切换均通过。

全程未修改主显示器、分辨率或刷新率。显示排列已经恢复原状，系统设置窗口已关闭，Computer Use 会话已重置。

## 物理拔插与重连实测

在安装版 PID `71088` 持续运行时，物理拔出并重新接入外接显示器 `H27E6`：

1. 重连后外接屏恢复为 display ID `2`，物理 UUID 仍为 `28556D51-AE91-4A85-87A1-6315582802DA`，稳定身份没有变化。
2. 双屏 frame 自动收敛回内建屏 `{{0, 0}, {1710, 1107}}`、外接屏 `{{1710, -333}, {2560, 1440}}`，没有改变主屏、分辨率或排列。
3. 顶屿进程仍为 PID `71088`，内建屏紧凑窗口仍为 `(667, 0, 377, 34)`；重连没有触发重启、迁屏或第二个窗口。
4. 重连后动画门禁通过：紧凑态 `377×34pt`、展开态 `460×190pt`、悬停响应 `55ms`、移出收回响应 `53ms`，12 秒保持和收回途中反向展开通过。

## 最终安装版

- Release 已重新构建并安装到 `/Applications/顶屿.app`，arm64 与 ad-hoc 严格签名验证通过。
- 最终安装版 PID `71088` 正常运行，登录项保持 `enabled / true`。
- 只读诊断确认物理显示器 UUID 为 `37D8832A-2D66-02CA-B9F7-8F30A301B230`，稳定身份为 `display-37d8832a-2d66-02ca-b9f7-8f30a301b230`。
- 最终安装版动画门禁通过：悬停响应 `60ms`、移出收回响应 `408ms`。

## 结论

显示 frame、缩放、外接屏排列以及物理拔插重连均已通过安装版实测；显示器热更新发布前待办关闭。
