# v0.1.2 设置与恢复入口验收

日期：2026-07-25

## 范围

- 常规页集中显示辅助功能、Apple Music 自动化、日历和提醒事项状态。
- 提供四项对应的 macOS 系统设置入口和“重新检查”操作。
- 提供带确认的“恢复当前屏幕默认布局”。
- 保留登录项、启动后显示、当前显示、折叠、展开预览和隐藏控制。

## 自动化与构建

- 完整 `swift test`：182 项通过，0 项失败。
- 新增两项布局恢复回归：全部默认参数会持久化；重置当前屏幕不会覆盖其他显示器。
- Release 构建、arm64、ad-hoc 严格签名和 `/Applications/顶屿.app` 安装通过。

## 安装版只读视觉验收

- 常规页显示登录项“已开启”、启动后显示和当前显示均开启。
- “维护与权限”正确显示：辅助功能已授权；Apple Music 等待应用运行；日历和提醒事项未请求。
- 四个权限恢复按钮、“重新检查”、当前显示器名称和“恢复当前屏幕默认布局”均完整可见。
- 页面说明明确：权限按钮只打开系统设置；布局恢复不修改音乐适配、登录项或其他屏幕。
- 上半区和下半区滚动、分组、按钮间距及文字换行正常。

本轮为只读视觉检查，没有执行布局重置，也没有改变任何系统权限。验收后已关闭顶屿设置和访达窗口，并重置 Computer Use 会话。

## 结论

v0.1.2 设置面板产品化、显示控制、当前屏幕布局恢复和权限恢复入口通过。
