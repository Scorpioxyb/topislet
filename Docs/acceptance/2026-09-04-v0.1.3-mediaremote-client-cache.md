# v0.1.3 M5 MediaRemote client 缓存候选验收

日期：2026-09-04

分支：`main`

基线提交：`c1482d2`

App：`0.1.3 (30)`，安装路径 `/Applications/顶屿.app`

## 本轮范围

- 汽水定向 MediaRemote client 按 Bundle ID 与目标 PID 缓存，复用前确认 Bundle ID、Parent Bundle ID、PID 和运行实例仍一致。
- 当前播放应用变化、状态流退出或目标失效时清理缓存；缓存读写使用轻量同步保护，避免抓取队列与通知回调并发访问。
- 不新增媒体轮询、第三条长期状态流、全局媒体命令或音频来源；完整元数据仍通过既有原子门禁统一发布。
- 网易云状态变化统一日志不再写入歌名，改用 12 位不可逆曲目标识。

## 自动化与供应链门禁

| 门禁 | 结果 |
| --- | --- |
| `git diff --check` | 通过 |
| `bash Scripts/rebuild-mediaremote-adapter.sh` | 通过；固定上游 commit 与补丁可逐字节重建 |
| Framework SHA-256 | `080c9cd816dcd7d5871a2c3a2b2012bdfe0ed03792c1c7b5b8d062d434b6f7f7` |
| Framework 架构 | `arm64` + `x86_64` |
| `swift test` | 224 项通过，0 失败 |
| `bash Scripts/verify-qishui-control-safety.sh` | 通过 |
| Release DMG 验证 | 版本、build、签名、架构、许可证、布局和校验和全部通过 |
| DMG SHA-256 | `5789d2c6e9a0e90794ff7ba4049eaf6d1d4350778d31a269334e498c7d601c85` |

## 安装版只读健康检查

- `/Applications/顶屿.app` 为 `0.1.3 (30)`，严格深度签名验证通过。
- 主进程单实例运行；长期子进程恰好为汽水与网易云两条预期只读 `stream-client`，未遗留临时诊断进程。
- 汽水定向读取返回 `mediaRemoteAvailable=true`、`verifiedQishuiSource=true`，Bundle ID 为 `com.soda.music`，目标 PID 与当前运行实例一致，封面数据非空。
- 汽水语义控制健康检查返回 `controlAvailability=available`、`allowsControl=true`。
- 本轮安装验证只执行只读检查，没有发送播放、暂停、上一首或下一首命令；三键真实运行态沿用同日 `2026-09-04-v0.1.3-qishui-three-control-runtime.md` 的已通过记录。

## 结论

M5 缓存候选通过代码、可复现构建、自动化、Release DMG 和安装版只读健康门禁，可以提交并推送 GitHub。缓存消除了每次状态抓取都重新枚举 MediaRemote client 的固定开销，但本轮没有新的 20 次主动切歌样本，因此不把既有“完整文本与封面 P95 不超过 500ms”目标标记为完成；元数据原子发布门禁继续保留。
