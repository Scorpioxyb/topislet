# v0.1.2 登录时自动启动验收

日期：2026-07-25

## 环境

- 安装位置：`/Applications/顶屿.app`
- Bundle ID：`io.github.scorpioxyb.topislet`
- 注册接口：`SMAppService.mainApp`
- 构建：Release、arm64、ad-hoc 签名

## 自动化

- 完整 `swift test`：180 项通过，0 项失败。
- 登录项专项覆盖系统状态映射、注册、注销、待批准、失败回滚和系统设置入口。

## 安装版实测

1. 初次注册返回 `loginItemStatus=enabled`、`loginItemRequested=true`、`error=nil`。
2. 独立状态读取保持 `enabled / true`。
3. 注销返回 `disabled / false / nil`，独立读取同样为 `disabled / false`。
4. 再次注册返回 `enabled / true / nil`，独立读取同样为 `enabled / true`。
5. macOS 后台任务数据库 `sfltool dumpbtm` 独立确认：
   - `Disposition: [enabled, allowed, notified]`
   - `Identifier: 2.io.github.scorpioxyb.topislet`
   - `URL: file:///Applications/顶屿.app/`
   - `Generation: 3`

## 结论

登录项注册、注销、状态同步和最终恢复开启均通过。顶屿当前保持登录时自动启动。

本轮没有强制注销当前用户；下一次用户正常重新登录时，只需观察顶屿是否自动出现，作为系统启动时机的补充验证。
